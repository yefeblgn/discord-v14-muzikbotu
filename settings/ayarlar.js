require("dotenv").config();

module.exports = {
    TOKEN: process.env.TOKEN || "",  // Bot tokeni 
    PREFIX: process.env.PREFIX || "!", // Bot prefixi
    SAHİP: process.env.SAHİP || "", // sahip id
}